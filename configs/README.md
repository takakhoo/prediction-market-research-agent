# Configs

This folder contains configuration, not code.

Subfolders:
1. `markets/`: market definitions and resolution-rule packs.
2. `sources/`: seed channels and source weights.
3. `risk/`: execution guardrails and limits.

Templates in this repository should be copied into runtime config files (ignored if sensitive).
